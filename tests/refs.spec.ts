import 'jest'
import { Document, Schema } from 'mongoose'

async function collectErrors(schema: Schema, doc: Document) {}

describe('Refs validation', () => {
  test('basic ref errors validation', () => {
    expect(true).toBeTruthy()
  })

})

